# Rejected source attempt

On 2026-08-26, the upstream Linux stable repository tag `v6.3.1` was fetched
while establishing the host.  It is not the required pmOS kernel baseline and
must not be used for this project.

The unmodified checkout was moved to the ignored local path
`cache/rejected/linux-stable-v6.3.1-incomplete/` for recovery or reference.
No build, patch application, boot-image creation, or device operation was
performed from that checkout.
