# OP3-AUDIO-MIC-001 — boot-failure correction checkpoint

```text
Task / GitHub Issue: https://github.com/iamgreatwk/op3/issues/5
Role: Implementation
Baseline commit: 67b0bbc3cbf46bae712a2606a43361756fcbd829
Working branch: agent/implementation/op3-audio-mic-001
Changed files: kernel/configs/oneplus3-audio.fragment; kernel/configs/pmos631/v612-v74strict-full.config (existing boot-proven base); scripts/prepare-op3-audio-kernel-config.sh; scripts/stage-op3-audio-rootfs.sh; boot/audio-test/opt/op3-audio/route.sh; patches/pmos612-op3-audio/; this handoff
Commit SHA: 2cc781600b01b18fbf0a21e394b8bb805577feee (audio integration assets)

Layer: Audio integration
Hypothesis tested: When the already-described OnePlus 3 ASoC topology has its complete ADSP, APR, SLIMbus, WCD9335, MSM8996 machine-driver and TFA9890 dependency chain built in, it registers ALSA controlC0 and the MultiMedia3 playback/capture PCMs after the provisioned ADSP starts.
Only variable changed: The pmOS 6.12 OnePlus 3 audio integration layer: its built-in kernel configuration and a runtime-validating route diagnostic helper. The follow-on test image appends only that helper and its launcher to the already firmware-proven initramfs; it does not replace or alter sda15/rootfs. The DTS topology, boot profile, ADSP firmware payload, GPU/DRM, Wi-Fi and other services remain unchanged.

Build run by project owner: Configuration preparation and `Image.gz dtbs`, 2026-08-31
Build result: PASS — the configured kernel Image.gz and OnePlus 3 DTB exist in the requested output directory.
Artifacts and SHA256: The prepared configuration is `out/linux-pmos-msm8996-6.12-defconfig-audio/.config`, SHA256 `56d2f1307bbd9cc502ab4b6f33e5b2b80387a0198f6137f6870c5de996a9ee52`. The build produced `Image.gz` SHA256 `cefc36a899aef73b3af50b53fd7a2d5dcfcc8f10b702b150f35c0eeffc1ed0a1` and `msm8996-oneplus3.dtb` SHA256 `cb29ab658135cd0cfcde3b47c1e115b763f5dbd37b724554590b7a61afcbf32f`. Static verification confirms `QCOM_Q6V5_ADSP`, `QCOM_APR`, `SLIMBUS`, `SLIM_QCOM_NGD_CTRL`, `SND_SOC_MSM8996`, `SND_SOC_WCD9335`, `SND_SOC_TFA989X`, and their QDSP6/REGMAP/RPROC closure resolve to `=y`; the compiled DTB retains `qcom,apq8096-sndcard` and its AMIC4 route.

Owner packaging: PASS (2026-08-31). `artifacts/boot-oneplus3-pmos612-own-dtb-audio.img` SHA256 `7c49bbe60944224b656dd0580b4cb875cbda0463fe3e40040c5ad7e4a814fae9`; full staged persistent rootfs archive `artifacts/op3-audio-rootfs.tar.gz` SHA256 `6006f5ffe09d48da2cb06ee6a93bf56d33cbe54a13c748938aa720a91a101f54`. Packaging PASS establishes only that the declared inputs produced an Android boot image and transfer archive. It does not establish kernel boot, ALSA registration, playback, capture, or suspend/resume behaviour.

Rootfs staging: PASS (owner-run, 2026-08-31). `BR2_PACKAGE_TINYALSA=y` and `BR2_PACKAGE_TINYALSA_TOOLS=y` are present in `out/buildroot-op3-egl/.config`. The staged target contains `usr/bin/tinymix` (`bd6b6dd37da7b969780846f8d09a33d4f386de54208c207cd5b7dd97a2da01c8`), `usr/bin/tinyplay` (`ce941c58cf43f0136b67a5397e40871b8afa2ee0ae3f722cc8c1f94909d666d5`), `usr/bin/tinycap` (`cd6b5c323680e668a73d33adcc82d275c01b1e81a0aa0328a165b40078f6bd24`), and `opt/op3-audio/route.sh` (`c1787698afc7d4b151df4089a5e4b1f38ba0e919a0a1d0cb61d8a590a20566a9`). This is rootfs assembly evidence only, not an ALSA or device PASS.

Initramfs generator self-test: PASS (agent-run, temporary path only). The first generator revision falsely rejected existing TinyALSA entries because `grep -q` closed a pipe under `pipefail`, causing `cpio` to report SIGPIPE. The corrected generator first materializes the archive listing, then checks it. It successfully appended `opt/op3-audio/route.sh` and `sbin/run_recovery.sh` to the firmware-provenance reference archive; no project artifact was created or overwritten during this self-test.

Minimal diagnostic packaging: PASS (owner-run, 2026-09-02). The corrected generator produced `artifacts/initrd-op3-audio-diagnostic.cpio.gz`, SHA256 `7cb3c3442317d89dba7727e5b82a3565146d7f330b2502e5081858c5bff0e206`. Packing that archive with the first, defconfig-based audio `Image.gz` and own DTB produced `artifacts/boot-oneplus3-pmos612-own-dtb-audio-diagnostic.img`, SHA256 `de2fbaf6ac491784b248eb0d8dce15da74e78596667be1066db1a78d5643c87c`. This establishes artifact assembly only; it is not a device boot, ALSA, playback, or capture result.

Corrected diagnostic rebuild: PASS (owner-run, 2026-09-02). The repaired configuration preparation generated `.config` SHA256 `2ebb875b6ed1694e91f51d078f6cb8d17e7c8a23850bc0252d74526de01e45b3`, exactly matching the boot-proven OP3-BOOT-042/044 own-DTB configuration. It retains S6E3FA5, UFS, ADSP, APR, SLIMbus, MSM8996, WCD9335, and TFA989X as built-ins. The rebuilt Image.gz is `3e0ff73edbeac89d57bc16db228dec285b18e36b8703a968ade04409e1f6059f`; the own DTB remains `cb29ab658135cd0cfcde3b47c1e115b763f5dbd37b724554590b7a61afcbf32f`; the minimal initramfs remains `7cb3c3442317d89dba7727e5b82a3565146d7f330b2502e5081858c5bff0e206`; and the replacement diagnostic boot image is `2625ed52fd90ac5e7162155b13e2040833516795aa568092c4464bb5e0d62a9f`. No device test of this corrected image has yet occurred.

Corrected device test: BOOT/INITRAMFS PASS; ALSA ENUMERATION FAIL (owner-run, 2026-09-02). The replacement image reached recovery/RNDIS/SSH and ran the launcher. ADSP firmware booted, APR services registered, SLIMbus reported its master capability and registered, and WCD9335 reported codec v2.0. The machine driver then failed: `msm-snd-apq8096 sound: error -EINVAL: MultiMedia4: error getting cpu dai name`, followed by `probe with driver msm-snd-apq8096 failed with error -22`. Consequently `/dev/snd` contains only `timer`, `/proc/asound/cards` reports no soundcards, no route log was created, and no speaker or microphone route was attempted.

Device test run by project owner: 2026-09-02, transient `fastboot boot` of `boot-oneplus3-pmos612-own-dtb-audio-diagnostic.img`
Device result: The first defconfig image failed the boot gate. The corrected image passed the boot/initramfs gate but failed ALSA card registration, before any route was enabled.
Evidence links / log paths: Owner terminal output: `/newroot/var/log/op3-audio-initramfs.log`, `/proc/asound/cards`, `/proc/asound/pcm`, and filtered dmesg. The missing `/newroot/var/log/op3-audio-route.log` is expected because `route.sh diagnose` aborted before it could open a mixer.

Conclusion: The defconfig-based preparation method is rejected for this task; it changed 6071 normalized configuration lines relative to the boot-proven own-DTB configuration, including the known-required S6E3FA5 panel setting. With that corrected, the audio hypothesis reaches a DTS-topology blocker: the compiled DTB's `q6asmdai` provider exports only DAI IDs 0–2 (MultiMedia1–3), while the available machine-card children include MultiMedia4–16. `qcom/common.c` iterates every available child and aborts at the first unavailable DAI, MultiMedia4. This is why all otherwise-ready audio components load but no card registers.
Authorized DTS correction: The owner authorized the audio-DTS topology correction on 2026-09-02. Kernel-source commit `955ea0e962134ad27b0fd6fc9b6945c6ffce4817` disables only `mm4-dai-link` through `mm16-dai-link` in the OnePlus 3 board DTS. Follow-up source commit `401a5673192be4239aecfddb5594f254d3d3e2a2` replaces only the invalid board audio-routing property. MultiMedia1–3, including the requested MultiMedia3 route, and all back-end links remain enabled. The source commits are archived in `patches/pmos612-op3-audio/`.

DTS-only diagnostic packaging: PASS (owner-run, 2026-09-02). Owner built DTBs from source commit `955ea0e`; the new OnePlus 3 DTB is SHA256 `bfacf07fb100d02cef76ee3bdae30f7a787edf4bdc4a21ed65153f24f2653288`. Static DTB inspection confirms MultiMedia4–16 have `status = "disabled"`, while MultiMedia3 remains available. Packing that DTB with the unchanged Image.gz and diagnostic initramfs produced `artifacts/boot-oneplus3-pmos612-own-dtb-audio-dts-diagnostic.img`, SHA256 `c85efa787b6d23940a277f644af7d056794ffde629545fd075ec85c24e2b4c11`. No device test of this new image has yet occurred.

DTS-only device test: BOOT/INITRAMFS PASS; ALSA CARD PARSE FAIL (owner-run, 2026-09-02). The MultiMedia4 failure disappeared, confirming the first DTS correction was effective. However, `qcom_snd_parse_of()` then reported three unresolved generic external-microphone routes: `Headset Mic -> MIC BIAS2`, `Primary Mic -> MIC BIAS1`, and `Noise Mic -> MIC BIAS3`. The parser returns the routing error before `devm_snd_soc_register_card()`, so no ALSA card appears. Source commit `401a5673192be4239aecfddb5594f254d3d3e2a2` replaces only the OnePlus 3 `audio-routing` property with its valid codec-side AMIC2/4/5-to-MIC-BIAS2/1/3 pairs.

Audio-route DTS packaging: PASS (owner-run, 2026-09-02). Owner built DTBs from source commit `401a567`; the OnePlus 3 DTB is SHA256 `ff377738d7ca1050e16fbdd22552caa90bddcf075e8509c38eccfb6c0c7a488b`. Static DTB inspection confirms `audio-routing` contains only RX_BIAS/MCLK and AMIC2/4/5-to-MIC-BIAS2/1/3 pairs, and MultiMedia4–16 remain disabled. Packing that DTB with the unchanged Image.gz and diagnostic initramfs produced `artifacts/boot-oneplus3-pmos612-own-dtb-audio-routes-diagnostic.img`, SHA256 `a16795a6bbf43c1ecbc25997f11167d7f5dab1e705305a5272144c8a484b7e7b`. No device test of this new image has yet occurred.

Audio-route DTS device test: PASS for ALSA enumeration (owner-run, 2026-09-02). The diagnostic launcher exited 0. `/dev/snd/controlC0` is present; card 0 is `OnePlus3` (`apq8096`); MultiMedia1–3 have playback and capture PCMs; MultiMedia3 is `hw:0,2`; and the Speaker tfa989x-hifi-5 playback back end is registered at device 5. ADSP, APR, SLIMbus and WCD9335 all reached their expected ready logs, with no ASoC error line. This is the ALSA-card gate only: no mixer control was changed and no sound was played or recorded.

TinyALSA CLI correction: The staged tinymix uses the command-form interface (`controls`, `get`, `set`), not the historical positional form. The diagnostic helper's bare `tinymix` call printed usage and its positional read/write calls would fail. The helper now uses `tinymix -D 0 controls`, `get`, and `set`; this is a userspace diagnostic-helper correction only, with no DTS or kernel change.
AMIC4 route mapping: Read-only device inspection established the exact controls: `ADC MUX4 = AMIC`, `SLIM TX4 MUX = DEC4`, `AIF1_CAP Mixer SLIM TX4`, and `MultiMedia3 Mixer SLIMBUS_0_TX`. The previous helper incorrectly used enum value `AMIC4`, a nonexistent `SLIM TX4` boolean, and an RX-style TX-mixer control name. It now uses the observed controls with command-form tinymix.
Uncertainties: Speaker output and AMIC4 capture remain untested. The AMIC4 helper's four controls have been read from the device but have not yet been enabled together.
Recommended next experiment: Rebuild only the diagnostic initramfs overlay containing the corrected AMIC4 helper, pack it with the fixed Image.gz and audio-route DTB, transiently boot it, then run `route.sh mic` followed by one 48 kHz, 16-bit, mono `tinycap` experiment on `hw:0,2`. Preserve the captured WAV and route log; no DTS, kernel, or persistent-rootfs change is part of this experiment.

AMIC4 realtime capture test: FAIL (owner-run, 2026-09-02). The owner transiently booted `boot-oneplus3-pmos612-own-dtb-audio-amic4-diagnostic.img` (SHA256 `8efbd1f53cbf52b312cfb9930ec270a91e2b8e90c5ce6aab6759224f7f624693`; initramfs SHA256 `2ae97b4eca3aa0dd4e6aee1b144ae0c9167bdc89fab4118408f973b511e6fb98`). `route.sh mic` completed with status 0, and readback confirmed ADC MUX4=AMIC, SLIM TX4 MUX=DEC4, AIF1_CAP Mixer SLIM TX4=On, and MultiMedia3 Mixer SLIMBUS_0_TX=On. A nominal 10-second `tinycap` run on card 0/device 2 at 48 kHz, 16-bit, mono returned 0 but wrote a 1.4 GiB WAV and reported 756,416,512 frames. At the same time, Q6ASM repeatedly logged `command[0x10bdb] not expecting rsp`; opcode `0x10bdb` is `ASM_DATA_CMD_EOS` in the pinned source. This violates the realtime-capture PASS condition, so the WAV must not be treated as valid audio and has not been replayed.

Next hypothesis: with the AMIC4 mixer route proven selected, capture completion is incorrect in the Q6ASM/ADSP protocol path. Preserve the diagnostic evidence and inspect bounded WAV samples plus the Q6ASM EOS response path before changing any source, DTS, mixer control, or rootfs input. The 1.4 GiB diagnostic artifact may be deleted only after the owner decides it is no longer required.

AMIC MUX4 control check: NO_CHANGE (owner-run, 2026-09-02). Static codec inspection shows that `ADC MUX4=AMIC` selects only the analog branch and that the branch contains `AMIC MUX4`, whose `ADC4` enum connects physical AMIC4. Device readback already selected `AMIC MUX4=ADC4` before the owner wrote the same value. A one-second repeat captured 68,829,184 frames into a 131.3 MiB WAV; separated data blocks still had the identical all-zero SHA256 `ad7facb2586fc6e966c004d7d1d16b024f5805ff7cb47c7a85dabd8b48892ca7`, and EOS acknowledgements continued. Thus AMIC MUX4 is not the changed variable and must not be added to the helper as a purported fix.

DAPM live-state check: PASS for the codec and SLIMbus capture path (owner-run, 2026-09-02). While `tinycap -D 0 -d 2 -c 1 -r 48000 -b 16` was running, MIC BIAS1, AMIC4, ADC4, ADC MUX4, SLIM TX4 MUX, AIF1_CAP Mixer, and AIF1 CAP all reported `On`; MultiMedia3 Capture and its `SLIM Capture` backend both reported `State: start`. The one remaining format discrepancy is explicit: the MultiMedia3 front end was configured for 1 channel, while SLIM Capture reported 2 channels at the same S16_LE/48 kHz. The earlier bare `tinycap -h` attempt used the default device rather than `-d 2`, so it does not test 2-channel MultiMedia3 capture. The next isolated runtime variable is the front-end channel count only.

Two-channel frontend check: FAIL (owner-run, 2026-09-02). With the same route and `hw:0,2`, changing only `tinycap` from 1 to 2 channels still wrote 160.9 MiB in one second (42,184,704 frames) and separated data blocks had the same all-zero SHA256 `ad7facb2586fc6e966c004d7d1d16b024f5805ff7cb47c7a85dabd8b48892ca7`. The 2-channel SLIM Capture backend therefore does not explain the invalid completion rate or silence. Upstream subsequently documented that `ASM_DATA_CMD_EOS`/`0x10bdb` responses arrive for reads pending when a stream is closed and added explicit handling for them; this explains the close-time error messages but does not make the running zero-buffer capture valid. Do not backport that log/close cleanup as a claimed microphone fix.

Next hypothesis: The invalid capture may be specific to the selected MultiMedia3 frontend rather than the shared SLIM Capture/codec path. A single manual differential test may switch the SLIMBUS_0_TX route from MultiMedia3/device 2 to MultiMedia1/device 0, with the codec route unchanged. If it produces valid realtime audio, update only the diagnostic helper's capture frontend; if it repeats the zero-buffer failure, retain the existing route and investigate the common Q6ASM/ADSP data path before proposing any source change.

Legacy-6.3.1 evidence review (2026-09-02; evidence only, not a baseline switch or patch authorization): the supplied v52 handoff records an earlier OnePlus 3 symptom of non-empty but all-silent AMIC4 WAVs. Its device-validated functional correction was to move the `gpio-gate-clock` provider `div1_mclk` from root `/clocks` to `&soc`, so the `gpio-clk` platform driver instantiates and gates physical codec MCLK through PM8994 GPIO15. It also records WCD9335 MIC-BIAS MCLK reference management and AMIC TX-hold release as separate historical codec changes. Current pinned 6.12 source still declares `div1_mclk: div1-clk` directly under root in `msm8996-oneplus-common.dtsi`, while the current device log says `qcom,slim-ngd-ctrl ... Failed to create device link (0x180) with div1-clk`. This is a direct reproduction of the historical precondition. The next source hypothesis, if separately authorized, is strictly DTS-only: move only `div1_mclk` to `&soc` while keeping its parent RPM divider, `audio_mclk` pinctrl, GPIO15, all existing audio routes, and `divclk4` unchanged. Do not infer that the historical WCD9335 edits apply until the DTS-only test result is known.

DTS-only MCLK-gate correction: PREPARED (owner authorized, 2026-09-02). Source commit `0591945aa3ddcc43e3fc2423e52e912462f65346` removes only the root-level `div1_mclk: div1-clk` node and recreates the identical `gpio-gate-clock` node under `&soc`. This allows `drivers/clk/clk-gpio.c` to bind it as a platform device. The RPM_SMD_DIV_CLK1 parent, PM8994 GPIO15 enable, `audio_mclk` pinctrl, `divclk4`, WCD9335 code, all existing DTS audio links/routes, Image.gz, initramfs and persistent rootfs are fixed. This is OP3-AUDIO-006; owner must build only DTBs, pack with the existing AMIC4 diagnostic initramfs, transiently boot, then first verify `div1-clk` binding/device-link removal before repeating a bounded capture. The patch archive is `patches/pmos612-op3-audio/0003-arm64-dts-qcom-oneplus3-instantiate-codec-MCLK-gate.patch`.

OP3-AUDIO-006 initial device result: PARTIAL (owner-run, 2026-09-02). The MCLK DTS-only image transiently booted. `/sys/bus/platform/drivers/gpio-clk/` now contains `soc@0:div1-clk`, linked to `/devices/platform/soc@0/soc@0:div1-clk`; debug clock summary exposes `div1-clk` at 19.2 MHz. However, early dmesg still reports `qcom,slim-ngd-ctrl ... Failed to create device link (0x180) with soc@0:div1-clk`. This proves the root-node instantiation defect is fixed but does not yet prove that the WCD9335 AMIC capture path prepares/enables MCLK. Do not repeat a long capture; next read the `div1-clk` counts while the existing bounded capture route is active.

OP3-AUDIO-006 live capture result: FAIL for MCLK request (owner-run, 2026-09-02). During a one-second MultiMedia3/SLIM Capture start with all AMIC4 DAPM widgets On, `div1-clk` still reported prepare=0 and enable=0. The capture remained invalid at 69,562,368 frames / 132.7 MiB with two zero data blocks. Thus moving the provider made it bind but the current card's capture graph does not request that clock. Historical v52's successful card additionally contains the valid codec-side audio-routing pair `MIC BIAS1`, `MCLK`; it is absent from the current minimal route override. The next distinct hypothesis, requiring a new owner authorization, is DTS-only and adds only that pair to the OnePlus 3 audio-routing property. It leaves `div1_mclk`, all mixer choices, other MIC BIAS routes, WCD9335 code, Image.gz, initramfs and rootfs unchanged. PASS requires a nonzero active MCLK count before evaluating capture payload.

OP3-AUDIO-007 DTS-only MCLK-request test: PREPARED (owner authorized, 2026-09-02). Source commit `0ba6a5392f169d9f5cb0dd860b9bcc076516d5d3` adds only `"MIC BIAS1", "MCLK"` immediately after the existing AMIC4-to-MIC-BIAS1 pair in the OnePlus 3 `audio-routing` property. The already-bound `div1_mclk` provider can therefore be requested by the active MIC BIAS1 DAPM path. `div1_mclk` placement, AMIC2/5 routes, mixer choices, WCD9335 code, Image.gz, initramfs and persistent rootfs are fixed. The archived patch is `patches/pmos612-op3-audio/0004-arm64-dts-qcom-oneplus3-request-MCLK-for-AMIC4.patch`. Owner must build DTBs only, pack with the unchanged AMIC4 diagnostic initramfs, transiently boot, then read `div1-clk` counts during the existing bounded capture before judging WAV content. PASS for this gate requires nonzero prepare/enable counts while capture is active; valid realtime payload remains a separate subsequent gate.

OP3-AUDIO-007 device result: PASS for the MCLK-request gate; FAIL for realtime capture (owner-run, 2026-09-02). With the unchanged AMIC4 route and a live one-channel 48 kHz MultiMedia3 capture, `div1-clk` reported prepare=1, enable=1, rate=19.2 MHz, enabled=Y and consumer `217:1a0:1:0 mclk`. MultiMedia3 Capture and SLIM Capture were both `State: start`. This directly validates the sole DTS variable: `MIC BIAS1` now requests the already-bound codec MCLK provider. However, the nominal one-second capture still returned 72,048,640 frames and produced a 137.4 MiB WAV, not a 48,000-frame stream. Its first sampled data block SHA256 is `2fcf7f71b235b6f0afac60e490fffa99b9ad523cfcd28bbd4be664e87b96946f`, different from the prior all-zero control, but this does not establish meaningful microphone samples. Do not claim capture/audio PASS. The next read-only step is to compare bounded blocks and sample values from this existing file; no new source, DTS, mixer, or rootfs variable is authorized by this result.

OP3-AUDIO-008 MultiMedia1 differential: FAIL for the frontend hypothesis (owner-run, 2026-09-02). Earlier MM1 attempts overlapped active streams and are discarded. After an explicit reboot and fresh transient boot of the unchanged OP3-AUDIO-007 image, the sole changed runtime variable was capture FE: MultiMedia3 SLIMBUS_0_TX=Off, MultiMedia1 SLIMBUS_0_TX=On, and `tinycap` used card 0/device 0. The AMIC4 codec path, 48 kHz mono S16_LE format, MCLK route, DTB, Image.gz and initramfs remained fixed. During the one-second stream, `div1-clk` remained prepare=1, enable=1, 19.2 MHz and enabled=Y; MultiMedia1 Capture and SLIM Capture both reported `State: start`. Nevertheless it returned 71,139,328 frames and wrote 135.7 MiB. Thus MultiMedia1 does not correct the invalid completion rate; the remaining common area is the Q6ASM/SLIM Capture data-completion path, not the selected front end. No source or DTS change is justified by this differential.

OP3-AUDIO-009 historical capture-buffer profile: FAIL for the TinyALSA buffer-geometry hypothesis (owner-run, 2026-09-02). With the already clean MultiMedia1 route and all kernel/DTS variables fixed, `tinycap` used the v52 profile `-p 480 -n 8 -t 5`. It returned exactly 241,920 frames (63 complete 3,840-frame rings) and created a 472.5 KiB WAV, but `/proc/uptime` advanced only 0.15 seconds, not five seconds. The bounded file size confirms this is not a storage runaway, while the exact whole-ring count demonstrates that read completions are being accepted as immediately available complete buffers rather than arriving at the 48 kHz sampling cadence. This rules out the default 1024×4 TinyALSA geometry as the cause. The next layer is Q6ASM `ASM_DATA_EVENT_READ_DONE_V2` observability: log the DSP completion status, token, returned address and current capture buffer before deciding any functional handling change.
```

OP3-AUDIO-010 WCD9335 AMIC TX-hold release: PREPARED (owner authorized, 2026-09-03). The two supplied Linux 6.3.1 evidence trees have the same local WCD9335 implementation: ADC pre-power-up asserts TX hold, then `wcd9335_codec_enable_dec()` releases the hold only at `POST_PMU`, after the selected decimator has configured and unmuted its TX path. The pinned 6.12 source asserted the hold but had no matching release. Source commit `2a97f48e6eb948fd05482c42a305c4da5e58d4c7` adds only that missing release: it obtains the AMIC number from the current decimator and calls `wcd9335_codec_set_tx_hold(..., false)`. DTS topology/MCLK routes, Q6ASM data completion, mixer route, ADSP firmware, initramfs, capture front end and TinyALSA geometry remain fixed. The patch is archived as `patches/pmos612-op3-audio/0005-ASoC-wcd9335-release-AMIC-TX-hold-after-decimator-setup.patch`. No build or device test has been run by the agent.

Owner build and test procedure: apply only archive patch `0005` to the active pmOS 6.12 source already carrying the previous `0001`–`0004` series; rebuild `Image.gz` with the existing `out/linux-pmos-msm8996-6.12-defconfig-audio` configuration, re-use the unchanged DTB and audio diagnostic initramfs, pack a new distinct boot image, then transiently boot it. Use exactly the known bounded MM1 capture condition: enable the AMIC4 route, set `MultiMedia3 Mixer SLIMBUS_0_TX=Off` and `MultiMedia1 Mixer SLIMBUS_0_TX=On`, then run `tinycap -D 0 -d 0 -c 1 -r 48000 -b 16 -p 480 -n 8 -t 5`. Record `/proc/uptime` before and after, TinyALSA's reported frame count, WAV size, selected mixer readbacks, active `div1-clk` row and a few separated payload hashes. PASS requires approximately five seconds of elapsed uptime, approximately 240,000 frames, and non-repeated/non-silent payload. If elapsed uptime remains near 0.15 seconds, OP3-AUDIO-010 fails irrespective of any amplitude change; do not combine a Q6ASM or PCM capability change in that repeat.

OP3-AUDIO-010 first boot: INCONCLUSIVE due to test-fixture failure (owner-run, 2026-09-03). The kernel registered card 0, WCD9335 v2.0 and every required current mixer control, including `ADC MUX4`, `SLIM TX4 MUX`, `AIF1_CAP Mixer SLIM TX4`, and both `MultiMedia1/3 Mixer SLIMBUS_0_TX` controls. However `/opt/op3-audio/route.sh` was SHA256 `c1787698afc7d4b151df4089a5e4b1f38ba0e919a0a1d0cb61d8a590a20566a9`, an obsolete helper that uses positional TinyALSA invocations and old route names (`AMIC4`, `SLIM TX4`, and reversed SLIMBUS mixer naming). It exits at the first AMIC control lookup, before setting the codec path. The committed staging helper is SHA256 `ab74a5de7ca5ecbde2cf64c04b1193b3266f35452152209e7083167bcb59ba1e` and uses the observed command-form controls (`ADC MUX4=AMIC`, `SLIM TX4 MUX=DEC4`, `AIF1_CAP Mixer SLIM TX4=1`, `MultiMedia3 Mixer SLIMBUS_0_TX=1`). The subsequent MM1 `-p 480 -n 8 -t 5` capture reported 241,920 frames while uptime advanced 2.98 seconds, but this is not a valid TX-hold result because the codec route was never configured. Rebuild only the diagnostic initramfs from the committed overlay, verify the helper hash after boot, and repeat the identical test; no kernel/DTS/Q6ASM/TinyALSA variable is added for that retry.

OP3-AUDIO-010 valid device result: FAIL for the AMIC TX-hold hypothesis (owner-run, 2026-09-03). After rebuilding the initramfs, `/opt/op3-audio/route.sh` matched the committed helper SHA256 `ab74a5de…` and completed all four expected settings: `ADC MUX4=AMIC`, `SLIM TX4 MUX=DEC4`, `AIF1_CAP Mixer SLIM TX4=On`, and `MultiMedia3 Mixer SLIMBUS_0_TX=On`. The owner then changed only the capture FE to MultiMedia1/device 0 and ran the established 48 kHz mono `tinycap -p 480 -n 8 -t 5` test. It again reported 241,920 frames and wrote 472.5 KiB, but uptime advanced from 66.08 to 66.24 seconds (0.16 seconds), not approximately five seconds. Therefore releasing TX hold after decimator setup does not correct the invalid capture completion cadence. Preserve source commit `2a97f48e` as failed evidence only; it is not part of the accepted baseline. The next isolated hypothesis remains Q6ASM `ASM_DATA_EVENT_READ_DONE_V2` observability, with the failed TX-hold change excluded from that next A/B.

OP3-AUDIO-011 Q6ASM READ_DONE observability: PREPARED (owner authorized, 2026-09-03). Commit `d39beb8f34780b0f2ba7631cd6250f78f73ce913` restores the pre-OP3-AUDIO-010 WCD9335 behavior, so the failed TX-hold change is excluded. Commit `71f994fb84ab16d3fc3745604b09e7a0a8fb64a2` then changes only `q6asm.c`: for each audio client it logs at most the first 16 synchronous capture `ASM_DATA_EVENT_READ_DONE_V2` responses after the existing address-validation check. Each line reports the bounded completion ordinal, DSP return status, token, returned address and expected mapped address. It does not change the event callback, `snd_pcm_period_elapsed()`, `q6asm_read()` requeue, buffer geometry, PCM capabilities, DTS, MCLK, mixer controls, firmware, initramfs or userspace. The archive patches are `0006-ASoC-wcd9335-restore-pre-TX-hold-test-behavior.patch` and `0007-ASoC-q6asm-trace-initial-capture-read-completions.patch`; both pass `checkpatch` with zero errors and warnings. On the source state used for OP3-AUDIO-010, apply `0006` then `0007`, rebuild Image.gz only, reuse the verified routefix initramfs, transiently boot, establish the same MM1 capture route, reproduce the bounded 48 kHz `-p 480 -n 8 -t 5` capture, and collect `dmesg | grep 'capture READ_DONE'`. The presence, status/token order and address matches are the only decision data; no outcome is a microphone PASS.

OP3-AUDIO-011 device result: PASS for bounded Q6ASM observability; realtime capture remains FAIL (owner-run, 2026-09-03). All 16 logged READ_DONE packets returned `status=0x0`; tokens cycle 0 through 7 and every returned address exactly equals its expected mapped buffer address. The first eight token completions are spaced at approximately 10 ms, matching 480 frames at 48 kHz. The next token-0 completion is also approximately 10 ms later, but token 1 is returned after about 12 ms and tokens 2 through 7 then collapse to roughly 70 microseconds apart. This directly proves the DSP protocol does not report an error and does not misaddress DMA, while ALSA is nevertheless handed an immediately completed ring after the initial correctly paced data. The invalid 241,920-frame/0.16-second capture is therefore not a codec route, MCLK, analog input, DMA address or explicit DSP-status fault. Read-only comparison with the supplied 6.3.1 successful source shows its Q6ASM initial read queue and READ_DONE requeue are otherwise identical; its capture hardware capability additionally declares `SNDRV_PCM_INFO_NO_REWINDS | SNDRV_PCM_INFO_SYNC_APPLPTR`. That distinct ALSA application-pointer synchronization semantic is the next candidate only if separately authorized; do not change DSP completion handling based on this diagnostic alone.

OP3-AUDIO-012 capture application-pointer synchronization: PREPARED (owner authorized, 2026-09-03). Source commit `3be5def6f7c1c3216fdd6f844be655ac9df10532` changes only `q6asm_dai_hardware_capture.info`, adding the legacy pair `SNDRV_PCM_INFO_NO_REWINDS | SNDRV_PCM_INFO_SYNC_APPLPTR`. The first flag disallows backwards application-pointer movement; the second requires the ALSA client to synchronize its application pointer explicitly instead of relying on mmap status updates. This exact pair is present in the supplied 6.3.1 successful source, whereas initial capture read queueing and READ_DONE requeue are otherwise identical to current 6.12. Playback capability, all Q6ASM READ_DONE handling, Q6ASM diagnostic instrumentation, PCM geometry, DTS, MCLK, mixer route, firmware, initramfs and TinyALSA binary are fixed. The patch archive is `0008-ASoC-q6asm-synchronize-capture-application-pointers.patch`, which passes `checkpatch` with zero errors and warnings. Apply it after the current OP3-AUDIO-011 source state, rebuild Image.gz only, reuse the routefix initramfs, and repeat the established MM1 `tinycap -D 0 -d 0 -c 1 -r 48000 -b 16 -p 480 -n 8 -t 5` test. PASS requires both roughly five seconds elapsed uptime and no post-first-ring burst in the retained bounded READ_DONE trace; a TinyALSA compatibility error is a valid FAIL result, not a reason to alter userspace in the same task.

OP3-AUDIO-012 device result: FAIL for the legacy application-pointer synchronization hypothesis (owner-run, 2026-09-03). The owner transiently booted the dedicated `audio-applptr-diagnostic` image and verified the committed route helper SHA256 `ab74a5de7ca5ecbde2cf64c04b1193b3266f35452152209e7083167bcb59ba1e`. The AMIC4 path completed all four expected writes, then the capture FE alone was selected as MultiMedia1/device 0 (`MultiMedia3 Mixer SLIMBUS_0_TX=Off`; `MultiMedia1 Mixer SLIMBUS_0_TX=On`). The fixed 48 kHz, mono, 480-frame x 8-period, 5-second TinyALSA capture reported 241,920 frames and produced a 472.5 KiB WAV, while `/proc/uptime` advanced only 0.18 seconds (34.33 to 34.51), not approximately five seconds. The retained trace proves the intended test kernel was active: all 16 `capture READ_DONE` lines were present, each status was `0x0`, token order was 0–7 repeated, and each DSP address matched its expected mapped buffer. The first ring was approximately 10 ms per completion; the second ring again collapsed after the first two responses. Thus `SNDRV_PCM_INFO_NO_REWINDS | SNDRV_PCM_INFO_SYNC_APPLPTR` neither produces a TinyALSA compatibility failure nor corrects the post-first-ring completion burst. Preserve source commit `3be5def6` and patch `0008` as failed evidence only; they are not an accepted microphone fix. Do not add a userspace or completion-handler change without a separately authorized, single-variable hypothesis.

OP3-AUDIO-013 capture READ submission/response ordering: PREPARED (owner authorized, 2026-09-03). Before changing capture behaviour, source audit established that the 6.12 and supplied successful 6.3.1 `q6asm_read()` implementations are byte-for-byte equivalent in the relevant sequence: select `port->buf[dsp_buf]`, send `ASM_DATA_CMD_READ_V2` with `seq_id`/token equal to that index, advance modulo `num_periods`, and call it eight times during prepare plus once per READ_DONE. The legacy `.ack` callback is playback-only, so it cannot acknowledge capture buffers. Firmware is also not a remaining variable: `reference-initrd.img`, `v100-reference-initrd.img`, `reference-initrd-v56.img`, and the current firmware-provenance initramfs all contain `lib/firmware/qcom/msm8996/oneplus3/adsp.mbn` with SHA256 `fd3162d794fe67d8e10547a2392130037b96713155c363346d8630af1304fed9`. Source commit `f1c19271bba43adb609d5eaaf8613f884a7ff3a1` adds only a bounded, first-16 `capture READ_SUBMIT` log immediately before each `ASM_DATA_CMD_READ_V2` APR send; it retains the existing bounded READ_DONE log and changes no buffers, event handling, requeue, PCM capability, DTS, mixer, firmware, initramfs or userspace. The archive patch is `0009-ASoC-q6asm-trace-initial-capture-read-submissions.patch`. Rebuild Image.gz only, re-use the verified routefix initramfs, boot a distinct diagnostic image, and repeat the fixed MM1 capture. PASS for this observability task is a complete, interpretable ordering trace, not microphone acceptance. If submissions 8–15 follow their corresponding READ_DONE lines promptly yet completions still burst, the host-side requeue timing is excluded; if they are delayed or batched before the burst, the host callback/transport scheduling remains implicated.

OP3-AUDIO-013 device result: PASS for submission/response observability; realtime capture remains FAIL (owner-run, 2026-09-03). The verified MM1 test again returned 241,920 frames in 0.15 seconds (uptime 50.11 to 50.26). Initial submits 0–7 were issued during prepare at 50.161596–50.161891. For each period-paced first-ring response, the driver submitted the replacement buffer promptly: READ_DONE 0 at 50.168885 was followed by submit 8 at 50.168953 (68 microseconds); the remaining callback-to-submit intervals were 62–155 microseconds. The second-ring responses then arrived at 50.248940, 50.260145, 50.262492, 50.262662, 50.262917, 50.263071, 50.263216 and 50.263360, with no further submissions needed in the bounded trace. Every completion still had `status=0x0` and the submitted/returned addresses matched. This excludes host-side delayed or batched q6asm requeue as the cause of the burst: the DSP/ASM side returns the newly submitted buffers prematurely after receiving them. The READ_DONE v2 payload contains only status and address, so it cannot establish how many samples the DSP actually wrote. No source behaviour was changed by this task.

OP3-AUDIO-014 single-outstanding capture READ diagnostic: PREPARED (owner authorized, 2026-09-03). The new source commit `5d6b7c49c6839c38cff8235cef4983242298cd8a` changes only initial capture queue depth in `q6asm_dai_prepare()`: instead of submitting `runtime->periods` READs before RUN, it submits one. The existing READ_DONE callback is unchanged and still submits exactly one replacement buffer per completion, so all later capture behaviour is identical except that there is never a full eight-buffer DSP read ring outstanding. The retained `0007`/`0009` traces remain active; no PCM hardware capability, period geometry, completion handling, mixer, DTS, firmware, initramfs or userspace change is included. The archive patch is `0010-ASoC-q6asm-test-single-capture-read-queue-depth.patch`. Repeat the established 48 kHz mono MM1 480x8/5-second capture unchanged. PASS for this diagnostic means elapsed uptime is approximately five seconds and the READ_DONE trace stays period-paced; it would identify full-ring requeue depth as causal, but not yet constitute an accepted production fix. If completion still bursts, full-ring queue depth is rejected and the evidence points to the active ASM/backend configuration rather than host queueing.

OP3-AUDIO-014 first device result: PARTIAL PASS for DSP completion cadence; FAIL for end-to-end capture (owner-run, 2026-09-03). The fixed TinyALSA 480x8/5-second command still reported 241,920 frames in 0.16 seconds (uptime 90.89 to 91.05), but this is no longer accompanied by a DSP completion burst. With one outstanding READ, every observed completion was period-paced: submit 0 at 90.948456 was completed at 90.955667, and submits 1 through 9 each completed approximately 9.84–10.04 ms later. Each completion caused the unchanged callback to submit the next token, cycling 0 through 7; all statuses and DMA addresses remained valid. Only ten READ_DONE/SUBMIT pairs exist because TinyALSA completed and stopped the stream after about 0.16 seconds, despite only roughly 4,800 frames having been reported through the driver. Therefore full-ring depth is a causal condition for the DSP-side premature-completion burst, but the remaining false userspace completion must be investigated separately through ALSA runtime pointers/availability. Do not accept the single-outstanding mode as a production configuration from this partial result.

OP3-AUDIO-015 capture pointer-accounting observability: PREPARED (owner authorized, 2026-09-03). The owner-provided live status sample during an unlimited MM1 48 kHz/480x8 capture reports a 3,840-frame buffer but `avail=7,200` and `avail_max=39,840`; its DSP READ trace remains correctly period-paced. This cannot describe a normal bounded capture buffer. Source commit `9de073b8b144565db524c885eb7252335aaa933a` retains the OP3-AUDIO-014 one-outstanding READ behavior and changes only diagnostics in `q6asm-dai.c`: the first 16 capture READ_DONE callbacks record `pcm_irq_pos` before/after their period-byte increment, and the first 16 `.pointer` calls record the raw position, whether the existing wrap reset was taken, the resulting position, and returned frame position. Counters reset only in prepare. No pointer arithmetic, queue depth, completion/requeue behavior, PCM flags/geometry, DTS, mixer, firmware, initramfs or userspace behavior changes. The patch archive is `0011-ASoC-q6asm-trace-capture-pointer-accounting.patch`; strict checkpatch passes with 0 errors and 0 warnings. No device test has occurred. Build Image.gz only, reuse the known routefix initramfs and DTB, and run the existing MM1 48 kHz/480x8 capture. PASS for this observability task is an interpretable bounded `capture PERIOD`/`capture POINTER` sequence that identifies whether the driver-side position is incremented, wrapped, or queried inconsistently; it is not microphone acceptance.

OP3-AUDIO-015 device result: PARTIAL PASS for q6asm pointer observability; FAIL end-to-end capture (owner-run, 2026-09-03). The owner built Image.gz with `CONFIG_EXTRA_FIRMWARE_DIR` supplied only as an environment override to the original source tree's untracked `extfw` directory, packed the pointer-trace image with the known routefix initramfs and DTB, and booted it transiently. The verified AMIC4/MM1 route again produced `Captured 241920 frames`, while uptime advanced only from 26.99 to 27.13 seconds. The bounded trace eliminates q6asm pointer arithmetic as the explanation: each real READ_DONE invokes one period callback that changes `pcm_irq_pos` by exactly 960 bytes, and the corresponding `.pointer` returns 480, 960, 1440, 1920, 2400, 2880, and 3360 frames before returning 0 at the expected 3,840-frame wrap. Immediately after that wrap, eight extra `.pointer` queries return 0 without another callback or DSP completion; READ_DONE 8 occurs only about 14.3 ms later and advances the next period normally. Thus the driver reports the expected pointer at every completion and the DSP remains period-paced with one outstanding READ. The impossible client completion must be below q6asm: ALSA core status/availability accounting (notably `hw_ptr_base`, `status->hw_ptr`, `appl_ptr`, or `__snd_pcm_lib_xfer()` at the wrap) is now the sole active layer. The in-tree `snd_pcm:hwptr` tracepoint cannot be used without a build because this audio configuration leaves `CONFIG_SND_PCM_XRUN_DEBUG` disabled. The next isolated task is bounded ALSA-core accounting instrumentation only; do not change Q6 completion, DTS, mixer routing, TinyALSA, or capture queue depth in that task.
