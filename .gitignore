# Local build products and device evidence; retain them outside version control.
/artifacts/
/cache/
/diag_archive/
/out/
/source/

# Editor and temporary files.
*.swp
*.tmp
*~

# Privacy: local-only content (credentials, scraping), never push.
/local/
